# Курс по сетям для DevOps инженера

## 📋 О курсе

Этот курс разработан для освежения и закрепления сетевых знаний DevOps инженера. Рекомендуется проходить раз в 6-12 месяцев для поддержания навыков в актуальном состоянии.

**Длительность:** 5-7 дней (по 1-2 часа в день)

---

## День 1: Модель OSI и основы сетей

### Теория (45 мин)

#### Зачем изучать сети DevOps инженеру?
Каждое действие — открытие сайта, деплой приложения, подключение к базе данных — это работа сетей. Цель не просто запомнить команды, а **научиться мыслить как сетевой инженер** и находить проблемы методично.

#### Модель OSI — ваш главный инструмент мышления

OSI (Open Systems Interconnection) — это не реальный стек протоколов, а **логическая модель**, которая показывает, как данные передаются по сети шаг за шагом.

**Почему OSI критична для DevOps?**
- Помогает быстро локализовать проблему (L7? L4? L3?)
- Упрощает troubleshooting
- Используется в документации и при общении с командами

**Мнемоника для запоминания:**
- Снизу вверх: **P**lease **D**o **N**ot **T**hrow **S**ausage **P**izza **A**way
- Сверху вниз: **A**ll **P**eople **S**eem **T**o **N**eed **D**ata **P**rocessing

### 🔍 Уровни OSI для DevOps (от низу к верху)

#### 1️⃣ Physical (L1) — Физический уровень
**Что это:** Кабели, порты, сетевые карты, физическая инфраструктура
**Для DevOps:** Редко трогаешь напрямую, в облаке это зона провайдера

**Типичные проблемы:**
- Сервер "мертв"
- Нет link на интерфейсе
- Проблемы в дата-центре

**Инструменты:**
```bash
ethtool eth0              # Статус интерфейса
ip link show              # Состояние линка
dmesg | grep -i eth       # Ошибки сетевых карт
```

#### 2️⃣ Data Link (L2) — Канальный уровень
**Что это:** MAC-адреса, ARP, VLAN, bridge
**Для DevOps:** 
🔥 Часто всплывает в Docker, Kubernetes, виртуализации

**Типичные проблемы:**
- Контейнеры не видят друг друга
- ARP конфликт
- Неверный VLAN

**Инструменты:**
```bash
ip link show              # Интерфейсы
ip neigh                  # ARP таблица
arp -a                    # ARP кэш
bridge fdb show           # MAC адреса
tcpdump -e                # С MAC адресами
```

#### 3️⃣ Network (L3) — Сетевой уровень
**Что это:** IP, маршрутизация, NAT, ICMP
**Для DevOps:** 
🔥🔥🔥 **САМЫЙ ВАЖНЫЙ УРОВЕНЬ**

**Типичные проблемы:**
- Нет маршрута до сервиса
- Проблемы с NAT
- Pod не выходит в интернет

**Инструменты:**
```bash
ip route                  # Таблица маршрутизации
ping 8.8.8.8             # Проверка связности
traceroute google.com     # Путь пакета
mtr example.com           # Интерактивный traceroute
iptables -L -n -v        # NAT правила
```

#### 4️⃣ Transport (L4) — Транспортный уровень
**Что это:** TCP/UDP, порты, соединения
**Для DevOps:** 
🔥 90% проблем "приложение не отвечает" здесь

**Типичные проблемы:**
- Порт закрыт
- SYN_SENT зависание
- TIME_WAIT переполнение
- UDP пакеты теряются

**Инструменты:**
```bash
ss -lntup                 # Открытые порты
netstat -tuln             # Слушающие сокеты
nc -zv host 80           # Проверка порта
tcpdump port 443          # Фильтр по порту
conntrack -L              # Connection tracking
```

#### 5️⃣ Session (L5) — Сеансовый уровень
**Что это:** Сессии, keepalive, reconnect
**Для DevOps:** Часто сливается с L7, но важен для понимания

**Типичные проблемы:**
- Обрывы соединений
- Idle timeout
- Load balancer рвёт сессии

**Настройки:** Timeouts в NGINX/HAProxy, keepalive, WebSocket

#### 6️⃣ Presentation (L6) — Представительский уровень
**Что это:** Формат данных, шифрование, кодировка
**Для DevOps:** 
🔥 Критично для TLS/SSL

**Типичные проблемы:**
- TLS handshake failed
- Неверные сертификаты
- Старые cipher suites

**Инструменты:**
```bash
openssl s_client -connect example.com:443
curl -v https://example.com
# Cert-manager, Let's Encrypt, Vault
```

#### 7️⃣ Application (L7) — Прикладной уровень
**Что это:** HTTP, HTTPS, gRPC, DNS, SMTP
**Для DevOps:** 
🔥🔥🔥 **То, с чем работаешь каждый день**

**Типичные проблемы:**
- 502/503/504 ошибки
- Неверные HTTP headers
- Healthcheck падает

**Инструменты:**
```bash
curl -v https://api.example.com
dig example.com
nslookup example.com
grpcurl -plaintext localhost:50051 list
```

### 🧠 Алгоритм диагностики OSI для DevOps

Если сервис не работает, идём **сверху вниз**:

```
Не работает сервис →
  L7: curl вернул ошибку? (502/503/504)
  L6: TLS сертификат валиден?
  L4: порт открыт? (ss -lntup)
  L3: маршрут есть? (ip route)
  L2: интерфейс up? (ip link)
  L1: кабель подключен?
```

### 🧩 OSI в Kubernetes (практический пример)

| Уровень | Kubernetes компонент |
|---------|---------------------|
| L7 | Ingress, Envoy, Istio Gateway |
| L6 | TLS termination, Cert-manager |
| L5 | Service Mesh (Istio, Linkerd) |
| L4 | Service (ClusterIP, NodePort, LoadBalancer) |
| L3 | Pod CIDR, Service CIDR, kube-proxy |
| L2 | CNI (Calico, Flannel, Cilium), veth |
| L1 | Node network interfaces |

### IP адресация и подсети (30 мин)
- **IPv4**: структура адреса, классы (устаревшие)
- **CIDR нотация**: /24, /16, /8
- **Подсети**: как разделить сеть
- **IPv6**: базовое понимание

### Практика (60 мин)

#### 1. Диагностика по уровням OSI
```bash
# L1 - Физический уровень
ip link show                    # Интерфейсы UP?
ethtool eth0                    # Link detected: yes?
dmesg | tail -20               # Ошибки драйверов?

# L2 - Канальный уровень
ip neigh                        # ARP таблица
ip addr show                    # MAC адреса
bridge link show                # Bridge порты (для Docker)

# L3 - Сетевой уровень
ip route                        # Маршруты
ping -c 4 8.8.8.8              # ICMP доступность
traceroute google.com           # Путь пакета

# L4 - Транспортный уровень
ss -lntup                       # Открытые порты
nc -zv google.com 443          # TCP handshake
ss -s                          # Статистика сокетов

# L7 - Прикладной уровень
curl -v http://example.com      # HTTP запрос
dig example.com                 # DNS резолвинг
```

#### 2. Практический кейс: "Сервис не отвечает"

**Симуляция:** Nginx на порту 80 не отвечает

```bash
# Шаг 1: L7 - Приложение
curl http://localhost:80
# Ошибка: Connection refused

# Шаг 2: L4 - Порт слушает?
ss -lntup | grep :80
# Результат: порт не открыт

# Шаг 3: Nginx запущен?
systemctl status nginx
# Результат: inactive (dead)

# Решение:
systemctl start nginx
ss -lntup | grep :80
curl http://localhost:80
# Success!
```

#### 3. Калькуляция подсетей (L3)

**Задача:** Разделите сеть 10.0.0.0/16 на 4 равные подсети

```bash
# Исходная сеть: 10.0.0.0/16 (65536 адресов)
# Нужно 4 подсети → каждая /18 (16384 адреса)

# Подсеть 1: 10.0.0.0/18
#   Диапазон: 10.0.0.1 - 10.0.63.254
#   Broadcast: 10.0.63.255

# Подсеть 2: 10.0.64.0/18
#   Диапазон: 10.0.64.1 - 10.0.127.254
#   Broadcast: 10.0.127.255

# Подсеть 3: 10.0.128.0/18
# Подсеть 4: 10.0.192.0/18

# Онлайн калькулятор для проверки:
# ipcalc 10.0.0.0/16
```

#### 4. TCP vs UDP (L4)

**Эксперимент:**
```bash
# TCP: гарантированная доставка
nc -l 8080                      # Сервер
nc localhost 8080               # Клиент
# Попробуйте отправить сообщения

# UDP: без гарантий
nc -u -l 8081                   # UDP сервер
nc -u localhost 8081            # UDP клиент
```

### Контрольные вопросы

1. **Модель OSI:**
   - На каком уровне работает MAC-адрес?
   - На каком уровне работает IP-адрес?
   - Если curl возвращает "Connection refused", на каком уровне проблема?
   - Если ping проходит, но curl не работает, какой уровень проверить?

2. **IP адресация:**
   - Сколько хостов можно разместить в /24 подсети? (/28? /30?)
   - В чем разница между /16 и /24 сетью?
   - Что такое CIDR и зачем он нужен?

3. **TCP vs UDP:**
   - Когда использовать TCP, а когда UDP?
   - Что происходит при TCP handshake?
   - Почему DNS использует UDP для запросов?

4. **Практическая диагностика:**
   - Какую команду использовать для проверки открытых портов?
   - Как проверить, что между серверами есть маршрут?
   - Как посмотреть ARP таблицу?

**Ответы для самопроверки:**
<details>
<summary>Нажмите, чтобы увидеть ответы</summary>

1. OSI: L2 (MAC), L3 (IP), L4 (порт закрыт), L7 (приложение)
2. IP: 254 (/24), 14 (/28), 2 (/30); /16=65536 адресов, /24=256; CIDR для гибкой адресации
3. TCP: надежность; UDP: скорость; TCP=SYN,SYN-ACK,ACK; DNS: скорость важнее
4. ss/netstat; ping/traceroute; ip neigh/arp -a
</details>

---

## День 2: DNS и сетевая безопасность

### Теория (30 мин)
- **DNS**: A, AAAA, CNAME, MX, TXT записи
- **DNS резолвинг**: рекурсивный vs итеративный
- **Основы файрволов**: iptables, firewalld

### Практика (60 мин)
1. **Работа с DNS**
   ```bash
   # Детальная информация о DNS
   dig +trace example.com
   dig +short example.com A
   dig example.com MX
   
   # Проверка reverse DNS
   dig -x 8.8.8.8
   ```

2. **Настройка базового файрвола**
   ```bash
   # iptables - проверка правил
   sudo iptables -L -n -v
   
   # Разрешить SSH
   sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT
   
   # Разрешить HTTP/HTTPS
   sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT
   sudo iptables -A INPUT -p tcp --dport 443 -j ACCEPT
   ```

3. **Тестирование портов**
   ```bash
   # Проверка доступности порта
   telnet example.com 80
   nc -zv example.com 80
   
   # Сканирование портов (осторожно!)
   nmap -p 80,443 example.com
   ```

### Задание
Настройте локальный DNS сервер (dnsmasq или bind9) для тестового домена.

---

## День 3: Load Balancing и Proxy

### Теория (30 мин)
- **Load Balancing алгоритмы**: Round Robin, Least Connections, IP Hash
- **L4 vs L7 балансировка**
- **Proxy**: Forward vs Reverse proxy
- **HTTP/HTTPS**: Headers, методы, коды ответов

### Практика (60 мин)
1. **Настройка Nginx как reverse proxy**
   ```nginx
   upstream backend {
       server 10.0.1.10:8080;
       server 10.0.1.11:8080;
       server 10.0.1.12:8080;
   }
   
   server {
       listen 80;
       server_name example.com;
       
       location / {
           proxy_pass http://backend;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```

2. **Анализ HTTP трафика**
   ```bash
   # Детальный запрос с curl
   curl -v https://example.com
   
   # Только заголовки
   curl -I https://example.com
   
   # Указание конкретного заголовка
   curl -H "Accept: application/json" https://api.example.com
   ```

3. **Тестирование SSL/TLS**
   ```bash
   # Информация о сертификате
   openssl s_client -connect example.com:443 -showcerts
   
   # Проверка через специальные инструменты
   # testssl.sh example.com
   ```

### Проект
Разверните HAProxy или Nginx для балансировки между 3 веб-серверами.

---

## День 4: Контейнерные сети

### Теория (30 мин)
- **Docker networking**: bridge, host, overlay, macvlan
- **Kubernetes networking**: Pod network, Services, Ingress
- **CNI (Container Network Interface)**: Calico, Flannel, Weave

### Практика (60 мин)
1. **Docker сети**
   ```bash
   # Список сетей
   docker network ls
   
   # Создание кастомной сети
   docker network create --driver bridge my-network
   
   # Инспекция сети
   docker network inspect my-network
   
   # Запуск контейнера в сети
   docker run -d --network my-network --name web nginx
   docker run -d --network my-network --name app alpine sleep 3600
   
   # Проверка связности между контейнерами
   docker exec app ping web
   ```

2. **Kubernetes Services**
   ```yaml
   # ClusterIP Service
   apiVersion: v1
   kind: Service
   metadata:
     name: my-service
   spec:
     selector:
       app: myapp
     ports:
     - protocol: TCP
       port: 80
       targetPort: 8080
     type: ClusterIP
   ```

3. **Диагностика сети в кластере**
   ```bash
   # DNS внутри pod
   kubectl run tmp --rm -i --tty --image=busybox -- /bin/sh
   nslookup kubernetes.default
   
   # Проверка сетевых политик
   kubectl get networkpolicies
   
   # Логи kube-proxy
   kubectl logs -n kube-system -l component=kube-proxy
   ```

### Задание
Создайте микросервисное приложение с frontend, backend и database в разных Docker сетях.

---

## День 5: VPN и туннелирование

### Теория (30 мин)
- **VPN типы**: Site-to-Site, Remote Access
- **Протоколы**: OpenVPN, WireGuard, IPSec
- **SSH туннели**: Local, Remote, Dynamic port forwarding

### Практика (60 мин)
1. **SSH туннели**
   ```bash
   # Local port forwarding
   ssh -L 8080:localhost:80 user@remote-server
   
   # Remote port forwarding
   ssh -R 8080:localhost:3000 user@remote-server
   
   # Dynamic port forwarding (SOCKS proxy)
   ssh -D 9090 user@remote-server
   
   # Копирование через промежуточный хост
   ssh -J jumphost user@target-server
   ```

2. **Настройка WireGuard**
   ```bash
   # Генерация ключей
   wg genkey | tee privatekey | wg pubkey > publickey
   
   # Конфигурация сервера
   cat > /etc/wireguard/wg0.conf <<EOF
   [Interface]
   PrivateKey = SERVER_PRIVATE_KEY
   Address = 10.0.0.1/24
   ListenPort = 51820
   
   [Peer]
   PublicKey = CLIENT_PUBLIC_KEY
   AllowedIPs = 10.0.0.2/32
   EOF
   
   # Запуск
   wg-quick up wg0
   ```

3. **Анализ туннелей**
   ```bash
   # Проверка активных туннелей
   ip tunnel show
   
   # WireGuard статус
   wg show
   ```

---

## День 6: Мониторинг и troubleshooting

### Теория (30 мин)
- **Сетевые метрики**: bandwidth, latency, packet loss, jitter
- **Инструменты мониторинга**: Prometheus exporters, Grafana
- **Логирование**: syslog, журналы файрвола

### Практика (60 мин)
1. **tcpdump и Wireshark**
   ```bash
   # Захват трафика на интерфейсе
   sudo tcpdump -i eth0 -w capture.pcap
   
   # Фильтрация по порту
   sudo tcpdump -i eth0 port 80
   
   # Показать HTTP запросы
   sudo tcpdump -i eth0 -A 'tcp port 80 and (((ip[2:2] - ((ip[0]&0xf)<<2)) - ((tcp[12]&0xf0)>>2)) != 0)'
   
   # Захват только SYN пакетов
   sudo tcpdump -i eth0 'tcp[tcpflags] & tcp-syn != 0'
   ```

2. **Bandwidth тестирование**
   ```bash
   # iperf3 сервер
   iperf3 -s
   
   # iperf3 клиент
   iperf3 -c server-ip -t 30
   
   # Проверка задержки
   ping -c 100 -i 0.2 server-ip | tail -1
   ```

3. **Диагностика производительности**
   ```bash
   # Статистика сетевых интерфейсов
   netstat -i
   
   # Подробная статистика
   ip -s link
   
   # Мониторинг в реальном времени
   iftop -i eth0
   nethogs eth0
   ```

### Практический кейс
Диагностируйте проблему: пользователи жалуются на медленную загрузку веб-приложения.

---

## День 7: Cloud networking

### Теория (30 мин)
- **VPC (Virtual Private Cloud)**: подсети, route tables, security groups
- **Cloud Load Balancers**: ALB, NLB (AWS), Azure Load Balancer
- **Сервисные mesh**: Istio, Linkerd

### Практика (60 мин)
1. **AWS VPC (если доступно)**
   ```bash
   # Создание VPC
   aws ec2 create-vpc --cidr-block 10.0.0.0/16
   
   # Создание подсети
   aws ec2 create-subnet --vpc-id vpc-xxx --cidr-block 10.0.1.0/24
   
   # Security groups
   aws ec2 describe-security-groups --group-ids sg-xxx
   ```

2. **Istio Service Mesh (концептуально)**
   ```yaml
   # VirtualService для маршрутизации
   apiVersion: networking.istio.io/v1beta1
   kind: VirtualService
   metadata:
     name: reviews
   spec:
     hosts:
     - reviews
     http:
     - match:
       - headers:
           end-user:
             exact: jason
       route:
       - destination:
           host: reviews
           subset: v2
     - route:
       - destination:
           host: reviews
           subset: v1
   ```

3. **Multi-cloud connectivity**
   - Изучите концепции: VPN Gateway, Direct Connect, ExpressRoute
   - Практика: настройте VPN между двумя облачными провайдерами

---

## 🎯 Финальный проект

Создайте инфраструктуру со следующими компонентами и **задокументируйте, какой уровень OSI задействован**:

### 1. Multi-tier приложение
   - Frontend (Nginx) → **L7: HTTP, L4: TCP 80/443**
   - Backend API (в контейнерах) → **L7: HTTP/gRPC, L4: TCP 8080**
   - Database (PostgreSQL) → **L4: TCP 5432**

### 2. Сетевые требования
   - Load balancer перед frontend → **L4/L7 балансировка**
   - Внутренняя сеть для backend-database → **L3: приватные подсети, L2: VLAN/bridge**
   - NAT Gateway для исходящего трафика → **L3: NAT**
   - VPN для административного доступа → **L3: туннели, L6: IPSec/WireGuard encryption**

### 3. Безопасность
   - Firewall правила → **L3/L4: iptables**
   - Security groups / Network policies → **L3/L4: ingress/egress**
   - SSL/TLS терминация → **L6: TLS, L7: HTTPS**

### 4. Мониторинг
   - Сбор сетевых метрик → **L3/L4: bandwidth, connections**
   - Dashboard в Grafana → **L7: HTTP API**
   - Алерты на аномалии → **Все уровни**

### Диагностика финального проекта

При возникновении проблем, используйте OSI подход:

```bash
# Фронтенд недоступен (502 Bad Gateway)

# L7: Nginx работает?
curl -I http://localhost

# L4: Backend порт открыт?
nc -zv backend-host 8080

# L3: Маршрут к backend есть?
ping backend-host
traceroute backend-host

# L2: Интерфейсы контейнеров в одной сети?
docker network inspect my-network

# L1: Контейнеры запущены?
docker ps
```

### Документация проекта

Создайте network diagram, показывающий:
- Все компоненты
- IP адреса/подсети (L3)
- Порты (L4)
- Протоколы (L7)
- Security zones

---

## 📚 Чек-лист для повторения

После прохождения курса вы должны уметь:

**Концептуальное понимание:**
- [ ] Объяснить модель OSI и привести примеры с каждого уровня
- [ ] Применять OSI для диагностики проблем (top-down подход)
- [ ] Понимать, на каких уровнях работают основные технологии

**Практические навыки:**
- [ ] **L3:** Рассчитывать подсети и понимать CIDR
- [ ] **L3:** Диагностировать сетевые проблемы с ping, traceroute, mtr
- [ ] **L7:** Настраивать DNS записи и использовать dig
- [ ] **L3/L4:** Конфигурировать firewall правила (iptables)
- [ ] **L4/L7:** Разворачивать load balancer (nginx/haproxy)
- [ ] **L2:** Работать с Docker сетями и понимать veth, bridge
- [ ] **L3/L4:** Понимать Kubernetes networking модель
- [ ] **L3/L6:** Создавать SSH туннели и VPN
- [ ] **L2-L7:** Анализировать сетевой трафик с tcpdump
- [ ] **L6:** Работать с TLS/SSL сертификатами

**Troubleshooting:**
- [ ] Использовать OSI для методичной диагностики
- [ ] Читать и интерпретировать tcpdump вывод
- [ ] Находить проблемы с маршрутизацией
- [ ] Диагностировать проблемы с DNS
- [ ] Определять узкие места в сети

---

## 🔄 План повторения

### Через 6 месяцев
- Дни 1-3 (основы, DNS, Load Balancing)
- Финальный проект

### Через 12 месяцев
- Полный курс с акцентом на новые технологии
- Обновите знания по изменениям в Kubernetes, cloud платформах

---

## 📖 Дополнительные ресурсы

**Книги:**
- "TCP/IP Illustrated" - Richard Stevens
- "Computer Networking: A Top-Down Approach" - Kurose & Ross

**Онлайн:**
- [Networking Labs на KillerCoda](https://killercoda.com)
- [Cisco Networking Basics](https://www.netacad.com)
- [Kubernetes Networking Guide](https://kubernetes.io/docs/concepts/cluster-administration/networking/)

**Практика:**
- Создайте homelab с виртуальными машинами
- Участвуйте в CTF соревнованиях по сетям
- Экспериментируйте с GNS3 или Packet Tracer

---

## ⚡ Быстрая проверка знаний (15 минут)

Используйте эти вопросы для проверки перед собеседованием:

### Базовые концепции (OSI)
1. **Объясните модель OSI. На каких уровнях работают:** MAC-адрес, IP-адрес, TCP порт, HTTP?
2. **Ping работает, но curl не работает.** С какого уровня OSI начнёте диагностику?
3. **В Kubernetes pod не может достучаться до Service.** Какие уровни OSI проверите?

### Сетевые протоколы
4. **Объясните разницу между TCP и UDP.** Приведите примеры использования.
5. **Что происходит при вводе URL в браузере?** (DNS → TCP handshake → HTTP)
6. **Как работает NAT?** На каком уровне OSI?

### Load Balancing
7. **В чем разница между L4 и L7 load balancing?** Когда что использовать?
8. **Nginx: proxy_pass vs upstream.** В чем разница?

### Troubleshooting
9. **Как troubleshooting'ить медленное сетевое соединение?** (mtr, iperf, tcpdump)
10. **Команда возвращает "Connection refused" vs "Connection timeout".** В чем разница?

### Kubernetes
11. **Объясните Kubernetes Service types:** ClusterIP, NodePort, LoadBalancer
12. **Как работает kube-proxy?** На каких уровнях OSI?
13. **Что такое CNI?** Примеры CNI плагинов.

### Docker
14. **Как работает Docker bridge network?** На каких уровнях OSI?
15. **Контейнеры в разных Docker networks не видят друг друга. Почему?**

### TLS/SSL (L6)
16. **Как проверить SSL сертификат?** (openssl s_client)
17. **Что такое TLS handshake?** Какие уровни OSI задействованы?

### Дополнительные вопросы
18. **Что такое MTU и почему это важно?**
19. **Объясните разницу между hub, switch и router** в контексте OSI.
20. **Как работает ARP?** На каком уровне?

### Ответы для самопроверки
<details>
<summary>Краткие ответы</summary>

1. L2 (MAC), L3 (IP), L4 (TCP port), L7 (HTTP)
2. L4 → проверить порт (ss -lntup)
3. L3 (Pod IP), L4 (Service port), L7 (DNS в Kubernetes)
4. TCP: надежность, упорядоченность; UDP: скорость, real-time
5. DNS (L7) → TCP 3-way handshake (L4) → HTTP request (L7)
6. L3 (Network layer), изменяет IP адреса
7. L4: TCP/UDP порты; L7: HTTP headers, URL, cookies
8. proxy_pass: один upstream; upstream: балансировка между несколькими
9. mtr (задержки), iperf (bandwidth), tcpdump (packet loss)
10. Refused: порт закрыт (L4); Timeout: firewall/routing (L3/L4)
11. ClusterIP: внутри кластера; NodePort: на каждом node; LB: external
12. L3/L4: iptables/IPVS для маршрутизации к Pods
13. Container Network Interface, примеры: Calico, Flannel, Cilium
14. L2: виртуальный switch, veth пары; L3: NAT для external
15. Изоляция на L2, нужен routing или общая сеть
16. openssl s_client -connect host:443 -showcerts
17. Client Hello → Server Hello → Certificate → Key Exchange (L6)
18. Maximum Transmission Unit, фрагментация пакетов (L3)
19. Hub (L1), Switch (L2), Router (L3)
20. L2, резолвит IP → MAC адрес в локальной сети
</details>

Удачи в изучении! 🚀
