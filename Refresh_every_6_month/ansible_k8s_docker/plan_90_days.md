## Цель плана

За 90 дней выстроить **прочную DevOps-базу**, достаточную для junior/middle DevOps роли: уверенно работать с Linux и сетями, понимать Git, поднимать инфраструктуру через IaC, настраивать CI/CD и GitOps без «магии».

Основано на файле _«Карта навыков DevOps 2026: что учить первым, а что пока забыть»_.

---

## 🧱 Блоки обучения (в правильном порядке)

1. **Linux (фундамент)**
    
2. **Сети**
    
3. **Git**
    
4. **Infrastructure as Code (Terraform, Helm, Kustomize)**
    
5. **CI/CD и GitOps**
    

Антиприоритеты (на старте):

- Экзотические операторы Kubernetes
    
- Сложная observability-платформа (выше базовых метрик и алертов)
    
- «Платформа мечты» без понимания Linux и сетей
    

---

## 📆 План на 90 дней

Формат: **~1 час в день** (45–60 минут)

---

## 🔹 Дни 1–30: База (Linux + сети + Git)

### Цель

Научиться **чинить проблемы**, а не гуглить вслепую.

### Linux (основа)

**Темы:**

- Файлы и права: `chmod`, `chown`, `umask`
    
- Структура системы: `/etc`, `/var`, `/proc`, `/tmp`
    
- Процессы: `ps`, `top/htop`, `atop`
    
- Сервисы: `systemctl`, `journalctl`
    
- Сети: `ip`, `ss`, `netstat`
    
- Firewall: `iptables` / `nftables`
    
- Пакеты: `apt`, `dnf`
    
- Unit-файлы systemd
    

**Практика:**

- Найти процесс, который держит порт
    
- Поднять упавший сервис через `journalctl`
    
- Снять packet capture через `tcpdump`
    

---

### Сети

**Темы:**

- TCP handshake, MTU, congestion
    
- DNS: A / AAAA / CNAME / TXT / TTL
    
- HTTP/1.1 vs HTTP/2 vs HTTP/3
    
- TLS и цепочка доверия
    
- Диагностика: `dig`, `nslookup`, `mtr`
    
- Базовые облачные сети: VPC/VNet, security groups
    

**Практика:**

- Нарисовать сетевую схему проекта
    
- Найти и описать узкие места
    

---

### Git (база)

**Темы:**

- `git init`, `clone`, `status`, `add`, `restore`
    
- `.gitignore`
    
- `commit` (осмысленные сообщения)
    
- `branch`, `switch`, `merge`
    
- Merge Request / Pull Request
    
- Rebase vs Merge
    
- `rebase -i`, squash, fixup
    
- `stash`, `reset`, `revert`, `cherry-pick`
    
- Worktree
    

**Практика:**

- Превратить 7 мелких коммитов в 2 логичных
    
- Работать параллельно в двух ветках через `worktree`
    

---

## 🔹 Дни 31–60: Infrastructure as Code

### Цель

Повторяемая инфраструктура **без кликов мышью**.

### Terraform

**Темы:**

- Providers, state
    
- Modules
    
- Remote backend
    
- `plan` vs `apply`
    
- Dependencies
    
- `terraform validate`
    

**Практика:**

- Поднять VPC через модули
    
- Разделить окружения (dev/stage/prod)
    

---

### Helm

**Темы:**

- Charts
    
- Values
    
- Templates
    
- Dependency charts
    

---

### Kustomize

**Темы:**

- Base / overlay
    
- Patch вместо копипаста
    

---

### Policies

- Pod Security / OPA / Kyverno (на уровне понимания)
    

**Практика:**

- Оформить окружения через Kustomize
    
- Добавить политики в CI
    

---

## 🔹 Дни 61–90: CI/CD и GitOps

### Цель

Автоматическая доставка изменений до продакшена.

### CI/CD

**Темы:**

- Reusable workflows
    
- Templates, matrix, cache
    
- Secrets: OIDC federation (без вечных ключей)
    
- External Secrets
    

---

### Docker Images

**Темы:**

- BuildKit
    
- buildx
    
- Multi-arch образы (amd64 + arm64)
    

---

### GitOps

**Темы:**

- ArgoCD **v3**
    
- Sync policies
    
- Health checks
    
- Promotion окружений через PR
    

**Практика:**

- CI pipeline с matrix и cache
    
- GitOps: изменение манифеста → авто-обновление окружения
    

---

## 🧠 Финальный результат

К концу 90 дней ты:

- Не боишься Linux-серверов без GUI
    
- Понимаешь, где и почему «тормозит»
    
- Уверенно работаешь с Git
    
- Поднимаешь инфраструктуру кодом
    
- Настраиваешь CI/CD и GitOps
    
- Готов к реальному production, а не «учебному кластеру»
    

---

## 🧭 Принцип

> **Прод любит дисциплину, а не героизм**

Если проект срочно требует что-то конкретное — учи ровно то, что горит. Остальное по плану.
